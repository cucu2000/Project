import React from 'react';
import {View, Text} from "react-native";


const RollScreen = ({route}) => {
    return (
        <View style={{ flex: 1, padding: 24 }}>
          <Text> Hello </Text>
        </View>
    );
};

export default RollScreen;
